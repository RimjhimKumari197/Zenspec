window.onload = function () {
  document.getElementByClassName("email").value = ""; // Clears input on page load
  document.getElementByClassName("password").value = ""; // Clears input on page load

};
